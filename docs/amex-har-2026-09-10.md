# 2026-09-10 HAR 适配说明

分析的是本地 `global.americanexpress.com.har`，捕获时间为 2026-09-10 02:22:57–02:24:25 UTC。文件保持被 Git 忽略；本说明不保存原始请求、账户标识、Cookie、会话数据或 Offer 标识。

## 已观察到的变化

- `/offers` 的 `window.__INITIAL_STATE__` 为 JSON 字符串，内部使用未压缩的 Transit immutable map/list 和普通 map 表示。
- 卡片列表位于 `modules.axp-consumer-context-switcher.products.details.types.CARD_PRODUCT.productsList`；`axp-loyalty-root` 也包含该结构。可一次性读取，不必逐卡探测接口。
- 旧 `ReadCardAccountOffersList.v1` 的 LIST 响应只含 8 条，响应中的 eligible 计数却为 100。因此它不适合作为当前全部 Offer 的唯一来源。
- Offers Hub 页面已有数据位于 `modules.axp-loyalty-root.dataHooks.cache.axpOffersHub.result`：推荐列表 100 条、已添加列表 2 条，包含商户与 CARD 信息类条目。
- 捕获的官方 `axp-offers-hub` 前端代码使用 `ReadOffersHubPresentation.web.v1`。列表请求包括 `accountNumberProxy`、`locale`、`offerPage: "page1"` 和 `requestType`。可添加视图为 `OFFERSHUB_LANDING`，已添加视图为 `ADDEDTOCARD_LANDING`。
- 推荐列表使用 `recommendedOffers.offersList`。首页及登记响应的已添加摘要使用 `addedToCard.offersList`；已添加完整视图使用 `addedToCardViewAll.offersList`。两者不能混用，页名为 `page1` 等。
- `CreateOffersHubEnrollment.web.v1` 的捕获请求使用 `OFFERSHUB_ENROLLMENT`、`offerUnencrypted: false`、`synchronizeOnly: false`、`enrollmentTrigger: "OFFERSHUB_TILE"`。请求带 `ce-source: WEB`，没有 Bearer Authorization。
- 捕获的成功登记响应含 `status.purpose: SUCCESS`，且目标 Offer 在 `addedToCard.offersList` 中具有 `enrollmentDetails.status: ENROLLED`。
- 同一 HAR 也捕获了 `CreateCardAccountOfferEnrollment.v1` 的成功请求：body 包括 `accountNumberProxy`、`identifier`、`locale`、`requestDateTimeWithOffset`、`userOffset`，响应为 `isEnrolled: true`。该 identifier 后来出现在 Hub 已添加列表中，offerId 相同且 ENROLLED。两次登记发生在不同 Offer 上，不能推断必须双接口提交。
- 捕获的 Card 登记请求含 `ce-source: WEB` 和独立 correlation UUID，没有 Authorization/x-requested-with。v4.5 据此恢复旧登记协议，继续保留 Hub 全量读取；没有复制旧脚本猜 token 或假定 HTTP 200 成功的处理。

## 证据边界

v4.1 的解析修正直接依据同一 HAR 中官方前端的 `useAddedToCardViewAll()`：该函数返回 `data.addedToCardViewAll`，已添加视图从这里读取 `offersList.page1`；独立的 `useAddedToCardState()` 则读取首页摘要 `data.addedToCard`。v4.0 把摘要字段错用于完整视图，导致用户截图中的首卡解析错误。旧的合成测试也采用了错误的摘要结构，未能暴露该问题；现已更正并加入与官方 selector 的核对。

新增回归验证完整视图不回退读取摘要、7 张 whitelist 卡完成 14 个串行请求、空页成功解析、结构异常继续其他卡，并统计同一 Offer 在不同卡上的资格。这些是按官方字段构建的合成响应测试，不是用户真实 7 张卡已成功扫描的证明。

列表接口名称、请求参数与请求类型由同一 HAR 内捕获的官方前端代码支持；当前 HAR 没有直接记录 `ReadOffersHubPresentation.web.v1` 的浏览器请求，其首屏列表随服务端 HTML 返回。新扫描请求仍需真实页面验证，不能把离线适配称为已验证线上运行。

旧 `/api/servicing/v1/member` 在本次 HAR 中没有出现，只保留为手动卡片检测的一次性兼容 fallback；主路径读取已经观察到的页面卡片状态。

当前证据中 Offer 页数组全部包含在 `offersList` 中，没有观察到后续请求的服务器分页游标。脚本保留所有返回页；若响应明确出现额外服务器分页标记，会报告未完成，避免宣称完整扫描。

`node tests/amex/verify-har.cjs global.americanexpress.com.har` 已通过：识别 92 张卡片、100 条推荐和 2 条已添加数据，并核对目标登记确认。该检查没有发送网络请求，没有输出卡片标识。
